apiVersion: v1
kind: pod
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx


# kind: Pod
# apiVersion: v1
# metadata:
#   name: nginx
# spec:
#   containers:
#   - name: nginx
#     image: nginx